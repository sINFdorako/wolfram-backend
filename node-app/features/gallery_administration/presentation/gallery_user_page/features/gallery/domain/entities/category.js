export class Category {

    constructor(name, path, backgroundImage, categoryImages) {
            this.name = name,
            this.path = path,
            this.backgroundImage = backgroundImage
            this.categoryImages = categoryImages
    }

}